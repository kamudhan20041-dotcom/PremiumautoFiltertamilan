# --- Core Bot Framework ---
Pyrogram>=2.0.106
TgCrypto
# uvloop makes the bot 2x-4x faster on Linux (Koyeb/VPS)
uvloop

# --- Database (MongoDB) ---
# Specific versions required for stability with Motor
pymongo[srv]==3.12.3
motor==2.5.1
umongo==3.0.1
dnspython

# --- Web Server & Networking ---
aiohttp
aiofiles
ujson
requests
beautifulsoup4

# --- Media & Parsing ---
# We use the git version because the pip version is outdated
git+https://github.com/cinemagoer/cinemagoer
marshmallow==3.14.1
humanize
pyshorteners
python-dotenv
pytz
wget

# --- Optional / Extra Features ---
# (Only keep these if your bot actually uses YouTube/Image editing)
Pillow
yt-dlp
youtube-search-python
speedtest-cli
psutil
numpy
